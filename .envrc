#!/usr/bin/env bash

# Auto-activate kronos-env when entering this directory
if command -v pyenv >/dev/null 2>&1; then
    export PYENV_VERSION=kronos-env
    eval "$(pyenv init -)"
    echo "✅ Activated pyenv environment: kronos-env"
fi
